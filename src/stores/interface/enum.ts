// 资源类型
export enum resourceType {
  FOLDER = 'folder',
  FILE = 'file'
}

// 文件类型
export enum fileType {
  JS = 'js',
  TS = 'ts',
  HTML = 'html',
  CSS = 'css',
  SCSS = 'scss',
  SASS = 'sass'
}

// 标签枚举
export enum tag {
  FILE_TREE = 'file-tree-box',
  EDITOR = 'editor-box',
  DIV = 'div'
}

export enum locationType {
  TL = 'tl',
  TR = 'tr',
  BT = 'bt'
}
